<nav class="bg-[#1A1A2E] p-4 shadow-md relative">
  <div class="container mx-auto flex items-center justify-between relative">
    <!-- Logo -->
    <a href="/">
      <img
        src="/logo.png"
        class="h-9 w-9 hover:scale-110 hover:underline transition-transform duration-300"
        alt="Logo Irene Dev Frontend Developer"
      />
    </a>

    <!-- Hamburger Menu Button -->
    <button
      id="menu-btn"
      class="block md:hidden p-2 rounded text-[#FFFFFF] hover:bg-[#3282B8] focus:outline focus:outline-[#3282B8]"
      aria-label="Toggle Menu"
    >
      ☰
    </button>

    <!-- Menu Items -->
    <ul
      id="menu"
      class="hidden md:flex flex-col md:flex-row md:gap-10 items-center absolute md:static top-full left-0 w-full md:w-auto mt-4 md:mt-0 text-xl text-[#E0E0E0] bg-[#1A1A2E] md:bg-transparent shadow-md md:shadow-none z-50"
    >
      <li>
        <a
          href="/"
          class="block px-4 py-2 hover:text-[#ACD8FF] focus:outline focus:outline-[#3282B8]"
        >
          Home
        </a>
      </li>
      <li>
        <a
          href="/projects"
          class="block px-4 py-2 hover:text-[#ACD8FF] focus:outline focus:outline-[#3282B8]"
        >
          Projects
        </a>
      </li>
      <li>
        <a
          href="/about"
          class="block px-4 py-2 hover:text-[#ACD8FF] focus:outline focus:outline-[#3282B8]"
        >
          About Me
        </a>
      </li>
      <li>
        <a
          href="/contact"
          class="block px-4 py-2 hover:text-[#ACD8FF] focus:outline focus:outline-[#3282B8]"
        >
          Contact
        </a>
      </li>
    </ul>
  </div>
</nav>

<script>
  if (typeof window !== "undefined") {
    const menuBtn = document.getElementById("menu-btn");
    const menu = document.getElementById("menu");

    // Toggle menu visibility
    menuBtn.addEventListener("click", (e) => {
      menu.classList.toggle("hidden");
      e.stopPropagation(); // Prevent event propagation to the document
    });

    // Close menu when clicking outside
    document.addEventListener("click", (e) => {
      if (!menu.contains(e.target) && !menuBtn.contains(e.target)) {
        menu.classList.add("hidden");
      }
    });
  }
</script>
